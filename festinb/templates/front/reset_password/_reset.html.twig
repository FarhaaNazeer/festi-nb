 {{ form_start(resetForm) }}
    
    {{ form_widget(resetForm.plainPassword) }}

    <div class="form__row">
        <button class="btn btn--white btn--large btn--orange-gradient">Réinitialiser le mot de passe</button>
    </div>
{{ form_end(resetForm) }}
